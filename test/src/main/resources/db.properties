url=jdbc:postgresql://localhost:5432/java_a_from_z
user=postgres
password=a
createTable=CREATE TABLE IF NOT EXISTS users(first_name VARCHAR(50) UNIQUE NOT NULL, last_name  VARCHAR(50) NOT NULL);
addUser=INSERT INTO users (first_name, last_name) VALUES (?, ?)
findUserByFN=SELECT last_name FROM users WHERE first_name = ?
updateLastName= UPDATE users SET last_name = ? WHERE first_name = ?
ifExist=select COUNT(*) from users where first_name = ?